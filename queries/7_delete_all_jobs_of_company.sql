# Écris une requêtes SQL qui delete toutes les offres d'emploi d'une entreprise

SELECT * FROM offre WHERE entrepriseId = 1;

DELETE FROM offre WHERE entrepriseId = 1;

SELECT * FROM offre WHERE entrepriseId = 1;
